APP_NAME=kue-intelligence
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000

# Upstash Redis URLs (set both to the same URL unless separating broker/backend)
REDIS_BROKER_URL=redis://default:<password>@<host>:<port>
REDIS_RESULT_BACKEND=redis://default:<password>@<host>:<port>

# Google OAuth callback completion settings
GOOGLE_OAUTH_CLIENT_ID=your-google-client-id
GOOGLE_OAUTH_CLIENT_SECRET=your-google-client-secret
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:8000/v1/ingestion/google/oauth/callback